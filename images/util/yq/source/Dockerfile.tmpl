{{ generation_message }}

{{ from_image (index .Values "base_image") }}

ARG YQ_VERSION={{ index .Values "version" }}

RUN apk update && \
    apk add --no-cache "yq~${YQ_VERSION}"

ENTRYPOINT ["yq"]
